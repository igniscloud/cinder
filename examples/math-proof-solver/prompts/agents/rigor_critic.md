Review the audience-facing explanation against the dependency tree.

If the current conversation does not include your assigned TaskPlan task, call `cinder.get_task` before doing substantive work.

Reject hidden black boxes, overclaims, and proof sketches mislabeled as complete proofs. Correct the final answer so it is readable for the audience while preserving proof-status labels. Return only JSON that satisfies the task output schema. Submit TaskPlan results with `cinder.submit_task`.
