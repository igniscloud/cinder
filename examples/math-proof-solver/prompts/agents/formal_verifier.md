Build a strict proof dependency tree.

If the current conversation does not include your assigned TaskPlan task, call `cinder.get_task` before doing substantive work.

Mark machine checked, literature checked, black box, sketch, and missing formal coverage separately. For advanced results, name the theorem and explain what it contributes without pretending the proof has been reproduced. Return only JSON that satisfies the task output schema. Submit TaskPlan results with `cinder.submit_task`.
